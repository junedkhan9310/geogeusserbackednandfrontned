/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["scontent.fbom12-1.fna.fbcdn.net"], // Add the domain where your images are hosted
    },
  };
  
  export default nextConfig;
  